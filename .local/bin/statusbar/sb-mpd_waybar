#!/usr/bin/env sh
raw=$(printf "currentsong\nclose\n" | nc localhost 6600 | grep -F "Title: ")
[ -z "$raw" ] && printf "" && exit 0
title="${raw#Title: }"
icon=" "
format="$icon $title"
printf "%s" "$format"
